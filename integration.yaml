# yaml-language-server: $schema=https://sdk.netlify.com/sdk/config-schema.json
config:
  slug: rimje6r1-milvus-faker
  name: MilvusFaker
  description: -l

  scopes:
    site: ["read", "write"]
    user: ["read", "write", "read", "write"]
